# Mira Version Index

Use this compact index only for upgrade scope or regression investigation.
Runtime work follows `SKILL.md`; detailed historical notes are never loaded by
default.

| Version | Scope |
|---|---|
| 1.0.0 | First release distribution: sanitized paths, portable TeX detection, private benchmarks removed |
| 0.13.4 | Removed active deferred multi-paper validation obligations and added contest-budget-bounded candidate screening with scoped expensive-comparison waivers |
| 0.13.3 | Removed retired problem fixtures and hard-coded repairs while retaining generic rigid-chain, collision, and continuous-refinement knowledge routed by mechanisms |
| 0.13.2 | Maintenance-path cleanup: external skill audits use a caller-supplied checkout and no longer assume a local `MathModeling-skills` repository |
| 0.13.1 | Lightweight correction: optional stateless change advice, no change gates or state files, and final submission checks limited to actual or explicitly configured violations |
| 0.13.0 | Explicit selective change-impact propagation and fail-closed, official-rule-driven submission compliance inside the existing four stages |
| 0.12.2 | Live Matplotlib layout QA with nonblocking warnings, strict/off policies, cached paired export, broad standard-artist coverage, and Mathodology attribution |
| 0.12.1 | Nonblocking four-stage cleanup: no default paper appendix, no AI/human approval sentinels, only explicit current FAIL or `requires_user_decision` blocks, and obsolete control/report files removed |
| 0.12.0 | Four-stage workflow: `analysis`, `modeling`, `implementation`, and `paper` replace the public numbered-phase and lettered-gate sequence |
| 0.11.0 | Contest-product-quality release with stronger compilation, page, evidence, solver-consistency, table, formula, citation, and numeric checks |
| 0.10.0 | Visual-evidence release with executable Python/MATLAB figure routing and render validation |
| 0.9.0 | Portable compact control plane, historical project registry, and repeatable smoke tests |
| 0.8.21 | Removed the active AI-use compliance/disclosure chain |

Detailed notes through 0.8.21 remain under
`references/archive/version-history/` strictly for historical lookup and
regression tests. Legacy phase, gate, appendix, and approval behavior recorded
there is not an active Mira contract.
